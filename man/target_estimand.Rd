% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{target_estimand}
\alias{target_estimand}
\title{Declare the target estimand}
\usage{
target_estimand(
  estimand,
  scale,
  sesoi,
  bias_scale_unit = NULL,
  max_width = NULL
)
}
\arguments{
\item{estimand}{Character. One-sentence statement of the target
estimand (quantity, population, scale).}

\item{scale}{Character. The scale on which the estimand is expressed,
e.g. \code{"latent-outcome standardized mean difference"}.}

\item{sesoi}{Numeric. The smallest effect size of interest, on the
estimand scale. Must be strictly positive. Target scenario rows are
simulated at the SESOI.}

\item{bias_scale_unit}{Numeric or \code{NULL}. The declared substantive unit
Delta used to scale target bias, estimator bias, and estimand drift:
each is (expectation - target) / \code{bias_scale_unit}. Defaults to the
SESOI.}

\item{max_width}{Numeric or \code{NULL}. Maximum acceptable confidence
interval width, if one is declared. Used as the precision criterion
by the estimation threshold profile; reported (not thresholded)
otherwise.}
}
\value{
An object of class \code{recovery_target}.
}
\description{
Step 1 of the recovery-test protocol: state, in one sentence, the
quantity the study is designed to estimate, the scale on which it is
expressed, and the smallest effect size of interest (SESOI). The SESOI
should be justified on substantive grounds, not reverse-engineered from
the available sample size.
}
\examples{
target_estimand(
  estimand = "ITT mean difference at 12 weeks",
  scale = "latent-outcome standardized mean difference",
  sesoi = 0.40
)
}
\seealso{
\code{\link[=declare_recovery]{declare_recovery()}}
}
