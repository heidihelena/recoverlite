% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{measured_outcome}
\alias{measured_outcome}
\title{Declare the measurement model for the observed outcome}
\usage{
measured_outcome(reliability, evidence = NULL)
}
\arguments{
\item{reliability}{Numeric in (0, 1]. Declared reliability (e.g.
test-retest) of the outcome measure.}

\item{evidence}{Character or \code{NULL}. Evidence tier / source for the
declared reliability (e.g. a validation study). Echoed in the report.}
}
\value{
An object of class \code{recovery_measurement}.
}
\description{
Classical additive measurement error, per the manuscript (section 3.1):
the observed outcome is \code{y_observed = y_true + e} with
\code{Var(e) = 1/reliability - 1}, so that reliability =
Var(y_true)/Var(y_observed). Under this model the raw treatment
contrast is \emph{not attenuated in expectation}: measurement error inflates
residual variance (costing precision, power, and — downstream of low
power — Type M exaggeration), and is charged to the variance account,
not the bias account. (Attenuation by sqrt(reliability) would apply
only if the estimator standardized by the observed-outcome SD.)
}
\details{
If measurement is omitted from \code{\link[=declare_recovery]{declare_recovery()}}, the outcome is
treated as perfectly reliable and the report states this explicitly
(silence must not imply ideality).
}
