% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragility.R
\name{nuisance_fragility}
\alias{nuisance_fragility}
\title{Nuisance-parameter fragility curve}
\usage{
nuisance_fragility(
  design,
  parameter = c("attrition_rate", "reliability", "icc"),
  values,
  sims = 500,
  seed = NULL
)
}
\arguments{
\item{design}{A \code{\link[=declare_recovery]{declare_recovery()}} object.}

\item{parameter}{One of \code{"attrition_rate"}, \code{"reliability"}, \code{"icc"}.}

\item{values}{Numeric grid of parameter values to evaluate.}

\item{sims}{Simulations per grid point (default 500).}

\item{seed}{Optional integer seed.}
}
\value{
A data frame of class \code{recovery_fragility}.
}
\description{
Fragility curves over a nuisance parameter the diagnosis identifies as
binding (manuscript, section 2.2, Step 3): the verdict uses the
pre-specified point scenarios; these curves show where the verdict
would change. The chosen diagnosands are power, target-null-relevant
quantities being outside scope here, target bias, coverage, and
estimand drift across the grid, at the target effect (the SESOI).
}
