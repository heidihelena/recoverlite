% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{planned_analysis}
\alias{planned_analysis}
\title{Declare the planned analysis (answer strategy)}
\usage{
planned_analysis(
  estimator = c("linear_model", "lmm_random_intercept", "cluster_mean_ttest",
    "mi_baseline_adjusted"),
  formula,
  alpha = 0.05,
  inference = c("satterthwaite", "kenward_roger", "wald_z"),
  m_imputations = 20,
  degenerate_counts = FALSE
)
}
\arguments{
\item{estimator}{Character; one of the estimators above.}

\item{formula}{The planned model formula. The simulated data provide
\code{y_observed}, \code{treatment}, \code{baseline}, and (for cluster designs)
\code{cluster}. The treatment coefficient is the one whose name starts
with \code{"treatment"}.}

\item{alpha}{Numeric. Two-sided significance level of the planned
decision rule (default 0.05); also fixes the nominal \code{1 - alpha}
confidence interval whose coverage is diagnosed.}

\item{inference}{Character. Inference method for
\code{"lmm_random_intercept"}; ignored otherwise.}

\item{m_imputations}{Integer. Number of imputations for
\code{"mi_baseline_adjusted"} (default 20).}

\item{degenerate_counts}{Logical. Whether degenerate fits (singular or
boundary variance estimates; failure class (c) of the manuscript's
taxonomy) count against the model-failure threshold. Design-specific
and must be pre-specified; the choice is echoed in the report. Fatal
errors and nonconvergence always count; diagnostic warnings are
reported but never count. Default \code{FALSE} (degenerate fits are
recorded, retained, and their marginal effect on coverage reported
separately).}
}
\value{
An object of class \code{recovery_analysis}.
}
\description{
The analysis that will actually be applied to the observed data,
stated exactly — including the specific inference method, since
finite-sample behavior can differ sharply across methods that share a
model formula (manuscript, section 3.2).
}
\details{
Available estimators:
\itemize{
\item \code{"linear_model"} — \code{\link[stats:lm]{stats::lm()}} on the retained sample
(complete-case), Wald t interval. Baseline adjustment is expressed
through the formula, e.g. \code{y_observed ~ treatment + baseline}.
\item \code{"lmm_random_intercept"} — linear mixed model via \code{lmerTest::lmer()}
on the retained sample; \code{inference} selects \code{"satterthwaite"}
(default), \code{"kenward_roger"} (requires \code{pbkrtest}), or \code{"wald_z"}
(large-sample z interval).
\item \code{"cluster_mean_ttest"} — two-sample t-test on equal-weighted cluster
means (with cluster sizes fixed by design, equal and pupil-count
weighting coincide), \code{n_clusters - 2} degrees of freedom.
\item \code{"mi_baseline_adjusted"} — multiple imputation of missing outcomes
from a normal model drawing on (treatment, baseline), followed by the
baseline-adjusted linear model on each completed dataset and Rubin's
rules with Barnard-Rubin degrees of freedom. Identifiable for the ITT
estimand when dropout is MAR given the observed baseline; per the
manuscript (section 2.3), evaluated through bias against the target,
not through drift.
}
}
