% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thresholds.R
\name{recovery_thresholds}
\alias{recovery_thresholds}
\title{Versioned verdict threshold profiles}
\usage{
recovery_thresholds(
  profile = c("default", "strict", "lenient", "estimation"),
  null_rejection_mult = NULL,
  power = NULL,
  target_bias = NULL,
  coverage = NULL,
  type_s = NULL,
  type_m = NULL,
  model_failure = NULL,
  drift = NULL,
  overcoverage_flag = 0.975,
  mcse_margin = 2,
  min_conditional_n = 200,
  max_width = NULL
)
}
\arguments{
\item{profile}{One of \code{"default"}, \code{"strict"}, \code{"lenient"},
\code{"estimation"}.}

\item{null_rejection_mult}{Maximum target-null rejection rate as a
multiple of the nominal alpha.}

\item{power}{Minimum power at the SESOI.}

\item{target_bias}{Maximum absolute target bias in units of the
declared substantive scale Delta.}

\item{coverage}{Minimum coverage of a nominal 95\% interval (lower
bound; one-sided).}

\item{type_s}{Maximum Type S rate among significant estimates, when
stably estimable.}

\item{type_m}{Maximum Type M exaggeration among significant estimates,
when stably estimable.}

\item{model_failure}{Maximum counted model-failure rate (see
\code{\link[=planned_analysis]{planned_analysis()}} for which classes count).}

\item{drift}{Maximum absolute estimand drift in Delta units;
thresholded by the estimation profile only (reported, not
thresholded, in confirmatory profiles, where its effect is captured
by target bias and target-null rejection).}

\item{overcoverage_flag}{Coverage above this value is flagged as
inefficiency (never failure).}

\item{mcse_margin}{All required threshold margins must exceed this many
Monte Carlo standard errors for a PASS (default 2).}

\item{min_conditional_n}{Minimum contributing simulations for a
conditional diagnosand (Type S, Type M) to be treated as stable
(default 200 — a stability convention chosen so that the MCSE of a
conditional proportion cannot exceed ~0.035).}

\item{max_width}{Maximum acceptable confidence-interval width for the
estimation profile's precision criterion; usually inherited from
\code{\link[=target_estimand]{target_estimand()}} by \code{\link[=recovery_test]{recovery_test()}}.}
}
\value{
An object of class \code{recovery_thresholds}, carrying the
threshold-set version, the profile name, and a record of any values
changed from the shipped profile.
}
\description{
The recovery-test protocol ships threshold \emph{profiles} matched to design
goals (manuscript, section 2.5). Thresholds are conventions: their
function is not to be correct but to prevent post hoc negotiation with
borderline results. A profile must be selected or justified before
simulation; every deviation from a shipped profile is recorded and
echoed in the report, and the report recomputes the verdict under the
shipped strict and lenient profiles with signed margins to every
threshold.
}
\details{
The shipped confirmatory profiles are, exactly:\tabular{llll}{
   Criterion \tab Lenient \tab Default \tab Strict \cr
   Target-null rejection \tab <= 1.50 a \tab <= 1.25 a \tab <= 1.10 a \cr
   Power \tab >= 0.70 \tab >= 0.80 \tab >= 0.90 \cr
   Target bias \tab <= 0.10 D \tab <= 0.05 D \tab <= 0.025 D \cr
   Coverage \tab >= 0.900 \tab >= 0.925 \tab >= 0.940 \cr
   Type S \tab <= 0.05 \tab <= 0.01 \tab <= 0.005 \cr
   Type M \tab <= 2.00 \tab <= 1.50 \tab <= 1.25 \cr
   Model failure \tab <= 0.05 \tab <= 0.01 \tab <= 0.005 \cr
}


The rejection-rate threshold is one-sided: excess false claims trigger
failure, while conservative behavior is flagged through power and
precision. The coverage threshold is a lower bound; overcoverage above
\code{overcoverage_flag} (default 0.975) is flagged as inefficiency and
evaluated through precision, not treated as failure.

The \code{"estimation"} profile replaces the rejection-rate and Type S/M
criteria with target bias, coverage, precision against a declared
maximum acceptable width (see \code{\link[=target_estimand]{target_estimand()}}), and estimand
drift, evaluated on the target rows only.
}
