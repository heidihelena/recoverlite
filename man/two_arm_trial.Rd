% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{two_arm_trial}
\alias{two_arm_trial}
\title{Declare a two-arm randomized trial data strategy}
\usage{
two_arm_trial(
  n_per_arm,
  allocation = 0.5,
  baseline_outcome_cor = 0.5,
  noncompliance = 0
)
}
\arguments{
\item{n_per_arm}{Integer. Participants recruited per arm.}

\item{allocation}{Numeric in (0, 1). Share assigned to treatment.}

\item{baseline_outcome_cor}{Numeric in [0, 1). Correlation rho between
the observed baseline and the true outcome (default 0.5).}

\item{noncompliance}{Numeric in [0, 1). Share of treated participants
who do not receive treatment (one-sided never-takers). The target
estimand remains the declared ITT effect.}
}
\value{
An object of class \code{recovery_data_strategy}.
}
\description{
Individual random assignment to two arms, with an observed baseline
measure. The data-generating model follows the manuscript (section
3.1): for participant i with assignment Z,
\deqn{B_i ~ N(0,1)}
\deqn{Y*_i = tau Z_i + rho B_i + sqrt(1 - rho^2) eps_i}
so the true (latent) outcome has unit SD given Z and the declared
effect is expressed in latent standard-deviation units. The baseline is
observed at randomization for every participant and is available to the
answer strategy as the column \code{baseline}.
}
\details{
\code{n_per_arm} is the number of participants \emph{recruited} per arm (under
equal allocation); attrition, if declared via \code{\link[=attrition_model]{attrition_model()}}, is
applied after recruitment.
}
