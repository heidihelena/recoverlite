% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stopping.R
\name{recovery_test_stable}
\alias{recovery_test_stable}
\title{Run the recovery test under the algorithmic doubling stopping rule}
\usage{
recovery_test_stable(
  design,
  start_sims = 2000L,
  max_sims = 16000L,
  scenarios = "confirmatory_grid",
  thresholds = recovery_thresholds(),
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{design}{A \code{\link[=declare_recovery]{declare_recovery()}} object.}

\item{start_sims}{Initial simulations per scenario row (default 2000).}

\item{max_sims}{Pre-declared maximum simulations per scenario row
(default 16000, i.e. up to three doublings from 2000).}

\item{scenarios}{\code{"confirmatory_grid"} (default) or \code{"target_grid"};
passed through to \code{\link[=recovery_test]{recovery_test()}}.}

\item{thresholds}{A \code{\link[=recovery_thresholds]{recovery_thresholds()}} object.}

\item{seed}{Optional integer seed for reproducibility.}

\item{verbose}{Logical; if \code{TRUE}, report each doubling to the console.}
}
\value{
A \code{recovery_result} (as from \code{\link[=recovery_test]{recovery_test()}}) carrying an
additional \code{"stopping"} attribute: a list with \code{start_sims},
\code{final_sims}, \code{max_sims}, \code{resolved} (whether the verdict was
determined before the ceiling), \code{hit_ceiling}, and \code{unresolved} (a data
frame of any required margins still within the MCSE band at the final
\code{S}).
}
\description{
Wraps \code{\link[=recovery_test]{recovery_test()}} with the pre-declared simulation stopping
rule of the protocol (manuscript, section 2.4). The number of
simulations \code{S} starts at \code{start_sims} and is doubled whenever a required
threshold margin is still within \code{mcse_margin} (default 2) Monte Carlo
standard errors of its threshold, up to a pre-declared ceiling
\code{max_sims}. The rule stops as soon as the verdict is determined: a stable
declared failure locks FAIL, or all required margins resolve (clean PASS
or determined RISK). Anything still within \code{mcse_margin} MCSEs of a
threshold at \code{max_sims} is reported as RISK: the run cannot resolve
whether the design clears the bright line, and that irresolution is
itself the finding.
}
\details{
Doubling reuses the same \code{seed}; each \verb{(seed, S)} run is fully
reproducible.
}
