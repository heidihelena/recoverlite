% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{cluster_trial}
\alias{cluster_trial}
\title{Declare a cluster-randomized trial data strategy}
\usage{
cluster_trial(
  n_clusters,
  n_per_cluster,
  icc,
  icc_pessimistic = NULL,
  allocation = 0.5,
  evidence = NULL
)
}
\arguments{
\item{n_clusters}{Integer. Total number of clusters randomized.}

\item{n_per_cluster}{Integer. Individuals measured per cluster.}

\item{icc}{Numeric in [0, 1). Declared intraclass correlation.}

\item{icc_pessimistic}{Numeric or \code{NULL}. The upper end of the
empirically documented ICC range for the outcome domain, used in the
pessimistic scenario rows. If \code{NULL}, the pessimistic rows default to
\code{icc + 0.10} and the report records the value as a package default
(the lowest tier of the evidence hierarchy). Declare it explicitly.}

\item{allocation}{Numeric in (0, 1). Share of clusters assigned to
treatment.}

\item{evidence}{Character or \code{NULL}. Evidence tier / source for the
declared and pessimistic ICC values (e.g. a published ICC range for
the outcome domain). Echoed in the report.}
}
\value{
An object of class \code{recovery_data_strategy}.
}
\description{
Cluster-level random assignment with individual outcomes and cluster
sizes fixed by design. For pupil i in cluster j,
\deqn{Y_ij = tau Z_j + u_j + e_ij}
with total individual-level variance standardized to 1 and
ICC = var(u) / (var(u) + var(e)). The declared effect is expressed in
individual-level (total) standard-deviation units. The simulated data
contain a \code{cluster} id column for use in the analysis formula, e.g.
\code{y_observed ~ treatment + (1 | cluster)}.
}
