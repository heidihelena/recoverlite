% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fragility.R
\name{effect_fragility}
\alias{effect_fragility}
\title{Effect-size fragility curve}
\usage{
effect_fragility(design, effects = NULL, sims = 500, seed = NULL)
}
\arguments{
\item{design}{A \code{\link[=declare_recovery]{declare_recovery()}} object.}

\item{effects}{Numeric vector of true effects to evaluate. Defaults to
an even grid from a quarter of the declared effect up to the declared
effect, with the SESOI included.}

\item{sims}{Simulations per effect value (default 500; increase for
smoother curves).}

\item{seed}{Optional integer seed.}
}
\value{
A data frame of class \code{recovery_fragility} with one row per
effect: power, Type M, and precision, each with its MCSE.
}
\description{
Shows how power, Type M exaggeration, and precision behave as the true
effect shrinks below the declared target, with all nuisance
assumptions held at their declared values. This is deliberately
\strong{outside the PASS/RISK/FAIL verdict}: shrinking the target effect
changes the inferential target, so pessimistic scenario rows perturb
nuisance assumptions only, and fragility to the effect size itself is
reported as a separate continuous curve.
}
